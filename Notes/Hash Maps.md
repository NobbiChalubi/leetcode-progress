# Hash Maps

## Concept
-- Computers uses hashing this allows us to search for things at an insanely fast speed
-- Basically items are given a key which lets the computer calculate where that spot it depending on the keys value, it just lets you find stuff very quickly 
-- Example: Two Sum